export const admin = (state) => state.admin;
export const auth = (state) => state.auth;
export const student = (state) => state.student;
export const sTeacher = (state) => state.sTeacher;
export const fTeacher = (state) => state.fTeacher;

export default {
  admin,
  auth,
  student,
  fTeacher,
  sTeacher,
};
